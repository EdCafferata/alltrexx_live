package aisdata;
// import API_KEY_P1 from metro-react-native-babel-preset;
//import {API_KEY_P1, API_KEY_P1} from "@env";


//public class MarineTraphic {}

// uses:  process.env.<keytag>
// require('dotenv').config()
// import { url, useSWR }from'useSWR';
// import * as aisNummers from "./aisNummers.json";

// public class MarineTraphic {
    // ophalen alle bekende aisnummers in de DB of een json lijst
    // const url = "http://localhost/api/aisdatas";
 //   const aisdata = new aisNummers([]);

    // ophalen data bij https://www.marinetraffic.com/

//    public let getAPI_KEY_P1() {
  //      return API_KEY_P1;
    //}

//    API_KEY_P1 = process.env.API_KEY_P1

//    const url = "https://services.marinetraffic.com/api/exportvesseltrack/API_KEY_P1/v:2/period:daily/days:3/mmsi:aisdata";

 //   console.log(process.env.API_KEY_P1, API_KEY_P1)

    // const fetcher = (...args) => fetch(...args).then((res) => res.json());
//    const fetcher = (...args) => fetch(...args).then((res) => res.json());

    // const {data, error} = useSWR([url, { refreshInterval: 50000 }, token], fetcher);
//    const {data, error} = useSWR([url, { refreshInterval: 3600}], fetcher);

    // const parkData = data && !error ? data.slice(0,350) : [];
//    const Data = data && !error ? data.slice(0,350) : [];
// }
